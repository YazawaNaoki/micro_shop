<%= render 'layouts/users_header' %>

<section class="hero is-danger">
  <div class="hero-body">
    <div class="container">
      <h1 class="title">
        退会確認画面
      </h1>
    </div>
  </div>
</section>

<section class="section">
  <div class="container">
    <div class="columns is-centered">
      <div class="column is-4">
        <div class="notification is-danger">
          <strong>お客様の情報は完全に削除されます。</strong><br>
          退会する場合は「<strong>退会する</strong>」を押してください。<br>
          退会しない場合は「<strong>戻る</strong>」を押してください。
        </div>
        <br>
        <%= link_to '戻る', user_path(current_user), class: "button is-primary is-fullwidth" %>
        <%= link_to '退会する', user_delete_path(current_user), method: :patch, class: "button is-danger is-fullwidth" %>
      </div>
    </div>
  </div>
</section>
